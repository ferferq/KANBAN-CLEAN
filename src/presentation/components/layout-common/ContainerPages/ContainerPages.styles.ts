import { COLORS } from '@/presentation/styles/colors';
import styled from 'styled-components';

export const ContainerPageStyle = styled.main`
  width: 100vw;
  height: 100vh;
  background-color: ${COLORS.GREEN_900};
`;
